services:
  schedule-generator:
    build: .
    ports:
      - "5001:5000"
    volumes:
      - ./output:/app/output
    environment:
      - FLASK_ENV=production
      - PYTHONPATH=/app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/"]
      interval: 30s
      timeout: 10s
      retries: 3